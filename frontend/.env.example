# Copy to .env.local for local dev. Never commit the real file.
VITE_LIVE_SESSION_GATEWAY_WS_URL=ws://localhost:8086/ws/session
VITE_MEMORY_URL=http://localhost:8085
VITE_ACTION_URL=http://localhost:8083

# Which Life Graph user this dashboard reads/writes -- until there's real
# auth, this stands in for "who's using the app."
VITE_DEMO_USER_ID=demo-user
